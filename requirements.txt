# Runtime dependencies for MomentShift (瞬变工坊)
# GUI toolkit: PyQt6 + PyQt6-Fluent-Widgets (package name is `qfluentwidgets`)
PyQt6>=6.6.0
PyQt6-Fluent-Widgets>=1.6.0
# Image compression engine (always-available backend for the Compress feature).
# oxipng / OptiPNG / mozjpeg are optional external binaries detected at runtime.
Pillow>=10.0.0
# v0.8.4 内置 FunASR 本地推理（裁剪版 funasr_onnx，仅需这三个运行时依赖；
# 模型不随软件分发，下载到 tools/funasr/）
numpy>=1.24.0
onnxruntime>=1.17.0
jieba>=0.42.1
# v0.8.21 「真暂停」：挂起 / 恢复整棵 ffmpeg 进程树。软依赖，缺失时
# core/proc_control 自动退回「只是不再派发新任务」的软暂停。
psutil>=5.9.0
