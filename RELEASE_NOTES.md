# MomentShift V0.9.0 测试版

> 傻瓜式 ffmpeg 多媒体处理工具 —— 转换 / 压缩 / AI 放大插帧 / 音频转文字，全部本地运行。

## 🎯 本版重点（V0.8.1 → V0.9.0 累积）

### 大组件「放大」深度修复
- **360×360 方块切割回归彻底修复**（真实引擎实测定位）：ncnn-vulkan 引擎显式 `-t` 参数会触发 partial-tile 拼接错位——统一改走引擎 auto 模式，并兼容旧引擎（pad 到 tile 整数倍 + 输出裁剪回原比例），图片 / 视频 / GIF 任意尺寸放大后画面连续。
- **概率性界面未响应修复**：引擎检测（`find_engine`）结果缓存，开始任务 / 切换引擎 / 重扫不再做重复磁盘遍历，操作流畅。
- **放大提速**：PNG 中转快速压缩、视频抽帧 `-compression_level 1`。

### 放大前后对比窗口（完全重写 + 多轮打磨）
- **叠放分割 / 左右并排**双模式；分割线由独立覆盖层绘制、悬停即跟随（不再需要按住鼠标）。
- **Ctrl+滚轮整体缩放**看细节：缩放期间自动暂停视频（只显示当前帧）、停止 500ms 后恢复，不卡不糊。
- **视频 / GIF 左右播放同步**：1s 轮询对齐 + 切换模式 / 恢复播放时强制对齐，帧级对比有效。
- **超大图支持**：QImageReader 预缩放解码，8K 级图片也能流畅显示。
- **背景水印标签**：灰色错位平铺 + 中间留白，左右标注「原媒体展示 / 处理后媒体展示」，内容区域外透出。

### 其他
- 折叠卡片动画改淡入淡出，彻底消除上下抖动（引擎卡 / 设置卡等）。
- 对比 / 播放引擎轻量化：弃用 libmpv（不随包分发、不依赖系统安装），视频走 Qt 内置 QtMultimedia（FFmpeg 解码），GIF 走 QMovie，零额外依赖。

## 📦 安装
- **绿色版**：下载 `MomentShift-Windows-Portable.zip` 解压即用（首次启动按提示下载 ffmpeg，或手动放入安装目录）。
- **安装包**：`MomentShift-Windows-Setup.exe`（NSIS 安装向导）。

## ⚠️ 测试版说明
- 本版本为测试版（prerelease），主要验证放大 / 对比窗口的稳定性与性能。
- 放大引擎（Real-ESRGAN / Waifu2x / RIFE 等）需在「关于 → 引擎卡」一键下载，或手动放入 `tools/<引擎名>/`。
- 若发现任何问题，欢迎提交 [Issue](https://github.com/GreenChennai/MomentShift/issues) 反馈，附上复现步骤与日志最有效。
